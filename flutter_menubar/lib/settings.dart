import 'package:flutter/material.dart';

class SETTINGS extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text("These are the settings"),
        centerTitle: true,
        backgroundColor: Colors.black54,
      ),
    );
  }

}