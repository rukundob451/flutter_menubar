import 'package:flutter/material.dart';

class SIGNUP extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text("This is where you sign up"),
        centerTitle: true,
        backgroundColor: Colors.orange,
      ),
    );
  }

}